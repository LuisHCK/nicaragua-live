<%= form_for(@market) do |f| %>
  <% if @market.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@market.errors.count, "error") %> prohibited this market from being saved:</h3>

      <ul>
      <% @market.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Nombre" %><br>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label "Icono"%><br>
    <%= f.text_field :icon %>
  </div>

  <div class="actions">
    <%= f.submit "Guardar", class:"btn-large green" %>
    <%= link_to "Volver", panels_path, class:"btn-large red" %>
  </div>
<% end %>
