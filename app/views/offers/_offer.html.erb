<div class="col-sm-6 col-md-3">
  <div class="thumbnail">
    <%= image_tag(offer.image, lazy:true) %>
    <div class="caption">
      <h3> <%= offer.titulo %> </h3>
      <p><%= offer.descripcion %></p>
      <b>Precio: </b>
      <% if offer.precio == 'cordoba' %>
      C$
      <% else %>
      $
      <% end %>
      <%= offer.precio %>
      <br>
      <p><%= link_to("Ver Más", offer_path(offer) , class:"btn btn-primary") %></p>
    </div>
  </div>
</div>
