<table class="table">
	<thead>
		<tr>
			<th data-field="id">Nombre</th>
			<th data-field="name">Icono</th>
			<th data-field="price">Editar</th>
		</tr>
	</thead>
	<tbody>
		<% categories.each do |category| %>
		<tr>
			<td> <%= category.name %> </td>
			<td> <%= icon(category.icon,style:'font-size:2rem;') %> </td>
			<td> <%= link_to "Editar",[:edit, category], class:"btn gree" %>
				<%= link_to 'Eliminar', category,
				data: {confirm: '¿Estas Seguro?'}, method: :delete, remote: true, class:"btn red", id:"btn-delete" %>
			</td>
		</tr>
		<% end %>
	</tbody>
</table>
