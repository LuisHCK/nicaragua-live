<%= simple_form_for(@offer_saved) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :offer %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
