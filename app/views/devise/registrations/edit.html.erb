
      <ul class="tabs tab-demo z-depth-1">
        <li class="tab col s3"><a href="#profile">Perfil</a></li>
        <li class="tab col s3"><a href="#user">Cuenta</a></li>
      </ul>


<div class="container">

<p id="notice"><%= notice %></p>
  <% if user_signed_in? %>
    <div id="profile" class="col s6"> <%= render "/profiles/form" %> </div>
    <div id="user" class="col s6"> <%= render "/devise/registrations/form" %> </div>
  <% elsif client_signed_in? %>
    <div id="profile" class="col s6"> <%= render "/clientprofiles/form" %> </div>
  <% end %>
</div>
